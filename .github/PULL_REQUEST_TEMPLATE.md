## What

<!-- One sentence: what does this change build/fix? -->

## Why

<!-- Interview-level reason: which stage/scenario does this serve? -->

## How verified

- [ ] `pytest -q` — ___ passed
- [ ] New tests follow RED → GREEN (failing first, then minimal implementation)
- [ ] No secrets committed (`.env` stays local)
